# 🤖 AI Agent Toolkit - Полная документация

## Обзор

AI-ассистент CRM теперь имеет **полный доступ** к amoCRM через 10 инструментов для чтения и записи данных.

---

## 📊 Инструменты (Tools)

### Чтение лидов

#### `getVIPLeads()`
Получить VIP лидов (бюджет ≥ 500K)

**Пример:**
```
Ты: покажи vip
AI: [вызывает getVIPLeads()]
    Нашёл 4 VIP лида с бюджетом от 500K:
    • Сделка #58482961 — 1,000,000₽ (1 день назад)
    ...
```

#### `getImportantLeads()`
Получить важные лиды (100K-500K)

#### `getRiskLeads()`
Получить застрявшие лиды (7+ дней без движения)

#### `getLeadDetails(leadId)`
Получить ПОЛНУЮ информацию о лиде:
- История создания/обновления
- Все задачи (активные + выполненные)
- Последние 3 заметки
- Контакты

**Пример:**
```
Ты: расскажи про лид 58482961
AI: [вызывает getLeadDetails(58482961)]
    Лид "Сделка #58482961":
    • Создан: 26.12.2025
    • Обновлён: 26.12.2025
    • Задач: 2 активных
    • Заметок: 5
    
    Последние заметки:
    1. "Клиент просил перезвонить"
    2. "Отправил КП"
    ...
```

---

### ✅ Задачи

#### `getTodayTasks()`
Задачи на сегодня (все активные)

#### `getOverdueTasks()`  
Просроченные задачи

#### `createTask(leadId, text, hoursUntilDeadline?)`
⭐ **СОЗДАТЬ ЗАДАЧУ**

**Параметры:**
- `leadId` - ID лида
- `text` - текст задачи
- `hoursUntilDeadline` - через сколько часов (по умолчанию 24)

**Пример 1: Простая задача**
```
Ты: создай задачу для лида 123 - позвонить клиенту
AI: [вызывает createTask(123, "позвонить клиенту", 24)]
    ✅ Задача создана для лида #123
    Дедлайн: 27.12.2025, 21:00
```

**Пример 2: Срочная задача**
```
Ты: создай задачу для лида 456 отправить КП через 2 часа
AI: [вызывает createTask(456, "отправить КП", 2)]
    ✅ Задача создана для лида #456
    Дедлайн: 26.12.2025, 23:00
```

**Пример 3: Умный поиск лида**
```
Ты: создай задачу для мобильного приложения - звонок
AI: Хм, не знаю ID. Сейчас найду...
    [вызывает getVIPLeads() или getRiskLeads()]
    Нашёл лид "⭐ DEMO: Мобильное приложение" #58482819
    [вызывает createTask(58482819, "звонок", 24)]
    ✅ Задача создана!
```

---

### 📝 Заметки

#### `addNote(leadId, note)`
⭐ **ДОБАВИТЬ ЗАМЕТКУ**

**Пример:**
```
Ты: добавь заметку к лиду 123: клиент запросил отсрочку
AI: [вызывает addNote(123, "клиент запросил отсрочку")]
    ✅ Заметка добавлена к лиду #123
```

---

### 🔧 Обновление лидов

#### `updateLeadStatus(leadId, statusId)`
⭐ **ИЗМЕНИТЬ СТАТУС**

Переводит лид в другой этап воронки

**Пример:**
```
Ты: переведи лид 123
 в статус "переговоры"
AI: [вызывает updateLeadStatus(123, <status_id>)]
    ✅ Статус лида #123 изменён
```

---

## 🎯 Примеры использования

### Сценарий 1: Создание задачи

```
👤 Пользователь: создай задачу

🤖 AI: Конечно! Для какого лида создать задачу? 
      Можешь назвать ID или имя лида.

👤 Пользователь: для VIP лида на миллион

🤖 AI: Сейчас найду VIP лидов...
      [вызывает getVIPLeads()]
      
      Нашёл лид "Сделка #58482961" с бюджетом 1,000,000₽.
      Что написать в задаче?

👤 Пользователь: позвонить и уточнить сроки

🤖 AI: Отлично! Создаю задачу...
      [вызывает createTask(58482961, "позвонить и уточнить сроки", 24)]
      
      ✅ Задача создана для лида #58482961
      📅 Дедлайн: 27.12.2025, 21:00
```

---

### Сценарий 2: Проверка истории лида

```
👤 Пользователь: что там с лидом на 300к?

🤖 AI: Сейчас проверю...
      [вызывает getImportantLeads()]
      
      Нашёл "⭐ DEMO: Мобильное приложение (300K)" #58482819.
      Получаю детали...
      [вызывает getLeadDetails(58482819)]
      
      📊 Лид "Мобильное приложение (300K)":
      • Создан: 26.12.2025
      • Последнее обновление: 26.12.2025 (сегодня!)
      • Активных задач: 1
      • Заметок: 3
      
      📝 Последние заметки:
      1. "Клиент просил отправить примеры"
      2. "Созвон назначен на завтра"
      
      Всё свежее, лид активный!
```

---

### Сценарий 3: Массовое action

```
👤 Пользователь: создай задачу "проверить статус" для всех vip

🤖 AI: Понял! Сейчас найду VIP лидов и создам задачи...
      [вызывает getVIPLeads()]
      
      Нашёл 4 VIP лида. Создаю задачи...
      
      [вызывает createTask(58482961, "проверить статус", 24)]
      [вызывает createTask(58482963, "проверить статус", 24)]
      [вызывает createTask(58482823, "проверить статус", 24)]
      [вызывает createTask(58482965, "проверить статус", 24)]
      
      ✅ Создано 4 задачи для VIP лидов!
      Дедлайн всех: завтра 21:00
```

---

## 🧠 Как AI выбирает инструменты

AI **автоматически** анализирует запрос и выбирает нужные tools:

| Запрос | Tools | Логика |
|--------|-------|--------|
| "покажи vip" | `getVIPLeads()` | Прямой запрос списка |
| "создай задачу для лида 123" | `createTask(123, ...)` | Есть ID → сразу создать |
| "создай задачу для vip лида" | `getVIPLeads()` → `createTask(...)` | Сначала найти, потом создать |
| "что с лидом 456?" | `getLeadDetails(456)` | Детальная информация |
| "добавь заметку: звонил" | `addNote(...)` | Запись данных |

---

## ⚙️ Конфигурация

### MaxSteps = 5

AI может сделать **до 5 шагов** в одном запросе:

```
Шаг 1: getVIPLeads()           - получить список
Шаг 2: getLeadDetails(123)     - детали первого
Шаг 3: createTask(123, ...)    - создать задачу
Шаг 4: addNote(123, "...")     - добавить заметку
Шаг 5: [формирование ответа]
```

---

## 🔒 Безопасность

### Что AI НЕ МОЖЕТ:

```typescript
❌ Удалять лиды
❌ Удалять задачи/заметки
❌ Изменять бюджет
❌ Менять владельца лида (пока - можно добавить)
```

### Что AI МОЖЕТ:

```typescript
✅ Читать все данные
✅ Создавать задачи
✅ Добавлять заметки
✅ Изменять статус (этап воронки)
```

---

## 🚀 Расширение

### Добавить новый инструмент:

```typescript
// В telegram.ts - handleConversationalQuery()

tools: {
    // ... существующие tools
    
    myNewTool: tool({
        description: 'Что делает инструмент',
        parameters: z.object({
            param1: z.string().describe('Описание параметра')
        }),
        execute: async ({ param1 }) => {
            // Логика
            return { result: 'Данные' };
        }
    })
}
```

### Примеры расширений:

```typescript
// Поиск лидов по тексту
searchLeads: tool({
    parameters: z.object({ query: z.string() }),
    execute: async ({ query }) => {
        const leads = await amocrm.getNewLeads();
        return leads.filter(l => 
            l.name.toLowerCase().includes(query.toLowerCase())
        );
    }
}),

// Получение контактов лида
getLeadContacts: tool({
    parameters: z.object({ leadId: z.number() }),
    execute: async ({ leadId }) => {
        const details = await amocrm.getLeadDetails(leadId);
        return details.contacts;
    }
}),

// Назначить ответственного
assignLead: tool({
    parameters: z.object({
        leadId: z.number(),
        userId: z.number()
    }),
    execute: async ({ leadId, userId }) => {
        await amocrm.updateLead(leadId, { responsible_user_id: userId });
    }
})
```

---

## 📊 Статистика инструментов

**Всего:** 10 tools  
**Read-only:** 6 (60%)  
**Write:** 4 (40%)  

**По категориям:**
- Лиды: 4 tools
- Задачи: 3 tools
- Заметки: 1 tool
- Обновления: 1 tool
- Другое: 1 tool

---

## 🎉 Итого

Теперь AI-ассистент - это **полноценный CRM менеджер** в Telegram! 

Можешь:
- ✅ Просматривать все данные
- ✅ Создавать задачи естественным языком
- ✅ Добавлять заметки
- ✅ Управлять лидами

Всё через обычный чат! 🚀
